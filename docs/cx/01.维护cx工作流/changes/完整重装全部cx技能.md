# 变更：完整重装全部 cx 技能

## 状态

进行中

## 关联对象

- 用例：`../00.用例.md`
- 设计：`../00.设计.md`
- 任务：`../tasks/04.同步双语安装包.md`

## 当前事实

- 安装脚本调用 `shskills install --force --clean` 更新技能。
- `shskills` 根据安装清单记录判断内容未变化时跳过技能。
- 安装清单记录存在但实际技能目录缺失时，安装脚本仍报告技能已是最新。

## 目标状态

- 中文和英文安装脚本每次运行都从远端 `main` 完整重装该语言的全部 cx 技能。
- 安装脚本不依据既有清单哈希省略任何 cx 技能。
- 安装结束前逐个核对技能目录、文件集合和文件哈希，并在不一致时原样失败。
- 修复以补丁版本发布，并从远端新版本重新安装中文 cx。

## 主要变化

1. 远端 `main` 克隆成功后取得当前语言的权威技能集合。
2. 安装前逐个卸载全部当前 cx 技能，再调用 `shskills` 完整安装。
3. 安装后逐个比较远端源技能与全局安装技能的文件集合和文件哈希。
4. 中文实现与文档完成后同步英文实现与文档。
5. 发行验证通过后发布补丁版本并执行真实全局安装核对。

## 顺序工作清单

| 顺序 | 原任务或文档 | 当前动作 | 状态 |
| --- | --- | --- | --- |
| 01 | `tasks/04.同步双语安装包.md` | 更新完整重装与安装后核对要求 | 完成 |
| 02 | `tools/install_cx_zh.ps1` | 实现中文技能完整重装与核对 | 完成 |
| 03 | `tools/install_cx_en.ps1` | 同步英文技能完整重装与核对 | 完成 |
| 04 | 双语说明与专题文档 | 更新当前安装行为 | 完成 |
| 05 | 发布文件 | 准备并发布补丁版本 | 进行中 |

## 文件范围

- 正式文档：`docs/cx/01.维护cx工作流/00.设计.md`、`docs/cx/01.维护cx工作流/tasks/04.同步双语安装包.md`、`docs/cx/docs/05.双语包同步.md`、`README.zh-CN.md`、`README.md`、`packages/zh/README.md`、`packages/en/README.md`
- 代码文件：`tools/install_cx_zh.ps1`、`tools/install_cx_en.ps1`
- 发布文件：`VERSION`、`packages/zh/manifest.json`、`packages/en/manifest.json` 和根目录发行说明
- 单元测试文件：无；cx 源仓库禁止单元测试。

## 验证方式

- 在临时目标目录执行中文安装脚本，删除技能目录后再次更新，确认全部技能均重新安装。
- 比较远端源技能与安装目标的技能目录、文件集合和文件哈希。
- 运行 `uv run --no-project python tools/validate_release.py .`。
- 运行 `git diff --check`。
- 推送 `main` 和版本标签后，从远端运行中文安装脚本并核对全局十五个技能。

## 完成动作

- 审查通过后删除本文件。
- 把删除提交到 Git，不建立完成变更档案。
