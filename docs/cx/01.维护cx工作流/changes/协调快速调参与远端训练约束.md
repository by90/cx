# 变更：协调快速调参与远端训练约束

## 状态

进行中

## 关联对象

- 用例：`../00.用例.md`
- 设计：`../00.设计.md`
- 任务：`../tasks/04.同步双语安装包.md`

## 当前事实

- 本地快速调参规则把较小最佳验证损失、较多验证损失有效改进次数、完成轮次尽量接近第 `120` 轮和最佳轮次尽量靠近第 `120` 轮作为四项联合最高优先级目标。
- 远端新增了默认提前停止忍耐轮数 `20`、学习率循环调度参数、`3–20` 天基础窗口、候选确定后的 `30`、`40`、`50`、`60` 天长窗口扩展和全量调参规则。
- 远端把跑满 `120` 轮且最后 `20` 轮仍有新最佳验证损失设为快速调参正常候选的硬准入条件，与当前快速调参的四项联合连续目标冲突。
- 本地与远端已经分别完成并删除各自的临时变更文件，当前合并结果尚未形成统一事实和统一验证证据。

## 目标状态

- 快速调参始终按四项联合最高优先级目标筛选五候选，跑满 `120` 轮和最后 `20` 轮是否有效改进只作为候选证据，不作为单项硬准入门槛。
- 保留远端不冲突的默认提前停止忍耐轮数 `20`、学习率循环调度参数和分阶段窗口预算。
- 用户固定的维度保持不变；用户固定窗口时跳过基础窗口搜索和长窗口扩展。
- 窗口允许搜索时，先在 `3–20` 天内形成初选五候选，再对初选最强候选执行四次长窗口扩展；窗口参数发生变化后联合复跑五个最终完整参数组合，再按四项联合目标确认最终五候选。
- 全量调参保留远端候选资格、循环调度和训练规则，但只有快速阶段允许搜索窗口时才要求接收长窗口扩展证据。

## 主要变化

1. 合并远端全量调参源文件和安装包镜像。
2. 以当前快速调参四项联合目标为主体，吸收远端不冲突的训练周期、调度器和窗口阶段规则。
3. 同步中文设计、任务和技能后，再同步英文技能及两套安装包镜像。
4. 重新验证双语包、规则矩阵、源与镜像、编码、Git 差异和统一审查证据。

## 顺序工作清单

| 顺序 | 原任务或文档 | 当前动作 | 状态 |
| --- | --- | --- | --- |
| 01 | `00.设计.md` | 区分快速调参联合目标与全量调参候选资格 | 进行中 |
| 02 | `tasks/04.同步双语安装包.md` | 更新统一当前实现要求和验证口径 | 待处理 |
| 03 | 中文调参技能与安装包镜像 | 合并快速与全量调参规则 | 待处理 |
| 04 | 英文调参技能与安装包镜像 | 同步等价英文规则 | 待处理 |
| 05 | 双语公开入口 | 复核当前快速调参摘要不回退 | 待处理 |
| 06 | 验证与统一审查 | 重建合并后证据并完成两道门禁 | 待处理 |

## 文件范围

- 正式文档：`../00.设计.md`、`../tasks/04.同步双语安装包.md`
- 中文技能源：`SKILLS/zh/cx-pytorch-quick-hpo/SKILL.md`、`SKILLS/zh/cx-pytorch-full-hpo/SKILL.md`
- 中文安装包镜像：`packages/zh/.agents/skills/cx-pytorch-quick-hpo/SKILL.md`、`packages/zh/.agents/skills/cx-pytorch-full-hpo/SKILL.md`
- 英文技能源：`SKILLS/en/cx-pytorch-quick-hpo/SKILL.md`、`SKILLS/en/cx-pytorch-full-hpo/SKILL.md`
- 英文安装包镜像：`packages/en/.agents/skills/cx-pytorch-quick-hpo/SKILL.md`、`packages/en/.agents/skills/cx-pytorch-full-hpo/SKILL.md`
- 双语公开入口：根目录和安装包 `README`、安装包 `AGENTS.md`、时间序列技能及镜像
- 单元测试文件：未声明

## 验证方式

- 验证快速调参的固定维度、搜索范围和四项联合最高优先级目标在全部中英文入口保持一致。
- 验证快速调参的默认忍耐轮数、循环调度参数、基础窗口和长窗口扩展只在未固定维度内生效。
- 验证全量调参保留远端当前规则，并对固定窗口不要求不存在的扩展证据。
- 比较中文与英文技能源及安装包镜像哈希。
- 运行中文技能包、中文安装包、英文技能包、英文安装包和单一事实来源验证工具。
- 检查修改文件编码、未解决冲突和 `git diff --check`。
- 本次未声明单元测试，不创建、不修改、不运行单元测试。

## 完成动作

- `$cx-review` 的交付物质量审查和完成证据门禁通过后删除本文件。
- 把删除提交到 Git，不建立完成变更档案。
