# SKILLS

This directory is the public skill source read by `shskills`.

```text
SKILLS/
|-- en/
|   `-- cx-*/SKILL.md
`-- zh/
    `-- cx-*/SKILL.md
```

Use `--subpath en` for the English pack and `--subpath zh` for the Chinese pack.
