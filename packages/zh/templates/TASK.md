# 任务 NN：TODO

## 任务编号

TODO

## 任务名称

TODO

## 对应用例

TODO：填写主成功场景主步骤或条件子步骤。

## 类或类型量具

TODO：填写本任务负责的一个类或一组类。

## 代码文件

TODO：填写本任务唯一涉及的代码文件。

## 单元测试文件（明确声明时）

未声明。只有用户请求、既有任务文档或变更文档明确声明单元测试或 TDD 时，才填写唯一对应的单元测试文件。

## 当前实现要求

1. TODO
2. TODO

## 实现纪律

- 任务文件命名为 `NN.中文任务名.md`，不要使用 `00.任务.md`。
- story 建立后，本任务文件不得因实现改向、代码错误或需求调整而被新增、删除或重命名。
- 实现方式变化时直接改写本文件，只保留当前有效要求。
- 完成本任务文档后只编辑一个生产代码文件。
- 默认不创建或修改单元测试。
- 默认完整面向对象、极简、复用优先，避免过长文件、过长变量名和重复实现。
- 构造函数和普通函数优先用默认参数表达配置默认值，例如 `path=Config.default_config_file()` 或 `batch_size=config.train.batch_size`。

## 验证命令

```bash
TODO
```

## 审查结论

TODO：填写 `$cx-review` 的交付物质量审查和完成证据门禁结论；任一阶段失败时本任务保持未完成。

## 状态

TODO：待处理、进行中或完成。
