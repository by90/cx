# 00.项目说明.md

## 项目目标

TODO：说明项目要解决的核心问题。

## 业务领域

TODO：说明项目中的关键业务概念、边界和术语。

## 技术约束

- Python、PyTorch 和 Rust 工作默认不使用单元测试或 TDD；只有用户请求、任务文档或变更文档明确声明时才创建或修改单元测试。
- 涉及状态、生命周期、不变量和领域协作时，必须使用完整面向对象或等价类型建模。
- 默认完成一个任务文档后只编辑一个生产代码文件；需要第二个代码文件时先拆成下一个任务。
- 代码必须极简、复用优先、低重复，避免过长文件、过长变量名和句子式标识符。
- 所有 cx 场景、任务、流程和变更文档只能放在 `docs/cx` 下。
- 代码、文档、教程、研究、设计或流程变更等交付物必须先通过 `$cx-review`，交付前再通过 `$cx-evidence` 证据审查。

## 场景索引

| 编号 | 主成功场景 | 路径 | 状态 |
| --- | --- | --- | --- |
| 01 | TODO | `docs/cx/01.TODO/` | planned |
