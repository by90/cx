# GUIDE.md

本指南说明 `001_python_ml_project` 示例功能组如何使用 cx 文档集。

## 常规用法

1. 从 `docs/INDEX.md` 找到 `docs/001_python_ml_project/`。
2. 先阅读 `ENGINEERING_SPEC.md` 和 `CHANGELOG.md`。
3. 当任务需要行为发现时，再阅读或更新 `BDD.md`。
4. 普通、非编程任务不要自行创建新的 BDD 场景；不确定时先询问用户。
