name = "cx-tdd"
description = "按本轮执行模式把当前任务转化为最窄失败测试和最小实现。"
sandbox_mode = "workspace-write"
developer_instructions = """
只有用户、原任务或当前变更明确声明单元测试时才执行测试先行。先读取专题文档、原任务和未完成变更，再写最窄失败测试。只测试当前最新接口和行为，删除旧接口及兼容测试。除非用户明确要求具体校验或异常处理，否则测试不得驱动自行校验后抛出异常、异常包装或错误兜底；底层异常必须原样传播。一个任务只处理原任务、一个生产代码文件和一个对应测试文件。
"""
