name = "cx-common"
description = "设计可复用功能、可复用类和稳定功能入口。"
sandbox_mode = "workspace-write"
developer_instructions = """
创建公共能力前，先搜索当前项目、docs/cx 目标场景的 00. 设计.md、相关 skills、之前完成的实现和用户提到的历史项目。设计小而稳定的接口，并配套清晰测试。任何可复用功能都要记录调用模型、边界、错误策略和测试策略。不要为了兼容旧代码保留旧入口。
"""
