name = "cx-tdd"
description = "Turn the current task into the narrowest failing test and smallest implementation according to the execution mode."
sandbox_mode = "workspace-write"
developer_instructions = """
Run test-first work only when the user, original task, or active change explicitly declares unit tests. Read topic documents, the original task, and unfinished change before the narrowest failing test. Test only the latest interface and behavior and delete old-interface compatibility tests. Unless the user explicitly requests one specific validation or error behavior, tests never drive added validation, error wrapping, or fallback; the original error propagates. One task handles its original document, one production file, and one matching test file.
"""
