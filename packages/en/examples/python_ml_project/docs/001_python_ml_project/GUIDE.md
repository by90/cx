# GUIDE.md

This guide explains how the `001_python_ml_project` example feature group uses the cx documentation set.

## Normal Use

1. Start from `docs/INDEX.md` and locate `docs/001_python_ml_project/`.
2. Read `ENGINEERING_SPEC.md` and `CHANGELOG.md` first.
3. Read or update `BDD.md` only when the task needs behavior discovery.
4. Do not create new BDD scenarios automatically for ordinary non-programming tasks; ask the user first when unsure.
